
import TextCompare from "@/components/TextCompare";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient pb-10">
      <TextCompare />
    </div>
  );
};

export default Index;
